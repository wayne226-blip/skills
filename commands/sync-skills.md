# Sync Skills to iCloud

Sync all Claude skills (CoWork + custom + commands) to iCloud Drive.

## Instructions

1. Run `python3 ~/Claude/scripts/sync-skills.py` and capture the output
2. Report: how many files were updated vs unchanged, and list any new/updated files
3. If any skills are listed as "Uncategorized", flag them so I can add them to the category map in `~/Claude/scripts/sync-skills.py`
