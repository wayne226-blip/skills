# Backup

Run the Claude config backup script and show results.

## Instructions

1. Run `bash ~/Claude/scripts/backup-claude.sh` and show the full output
2. Find the most recent backup folder in `~/Library/Mobile Documents/com~apple~CloudDocs/Claude-Backups/` and read its `README.md`
3. Show me a brief summary: date, how many items backed up, and list the contents
